package com.example.grabit.data.config

object AppConfig {
    const val BASE_URL = "https://fakestoreapi.com/"

    const val REGISTER_USER_ACCOUNT = "users"

    const val GET_ALL_USERS = "users"
    const val GET_USER_BY_ID = "users/{id}"

    const val GET_USER_CART = "carts/user/{userId}"

    const val GET_ALL_PRODUCTS = "products"
    const val GET_TOP_PRODUCTS = "products"
    const val GET_PRODUCT_DETAIL = "products/{id}"
}
